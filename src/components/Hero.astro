---
// Hero Section Component
// Per PROMPTS.md Prompt 1: Futuristic Hero with animated grid, glassmorphism badge, gradient headline
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Animated Grid Background -->
  <div class="absolute inset-0 animated-grid opacity-30"></div>
  
  <!-- Radial Glow Effects -->
  <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-cyan-500/10 rounded-full blur-3xl"></div>
  <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-lime-500/10 rounded-full blur-3xl"></div>
  
  <!-- Hero Content -->
  <div class="relative z-10 text-center px-6 max-w-5xl mx-auto">
    
    <!-- Glassmorphism Badge -->
    <div class="inline-flex items-center gap-2 px-5 py-2.5 mb-8 rounded-full glassmorphism hover-glow cursor-default">
      <span class="w-2 h-2 bg-lime-500 rounded-full animate-pulse"></span>
      <span class="text-sm font-medium text-white/90 tracking-wide">AI-Native Studio</span>
    </div>
    
    <!-- Main Headline -->
    <h1 class="text-5xl md:text-7xl lg:text-8xl font-bold font-display mb-6 leading-tight">
      <span class="text-gradient">We Engineer</span>
      <br />
      <span class="text-white">the Future.</span>
    </h1>
    
    <!-- Subheadline -->
    <p class="text-lg md:text-xl text-white/60 max-w-2xl mx-auto mb-12 font-body leading-relaxed">
      Building Algeria's next generation of digital products.
      <br class="hidden md:block" />
      Design. Development. Automation.
    </p>
    
    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
      <!-- Primary CTA -->
      <a 
        href="#ventures" 
        class="group relative px-8 py-4 bg-gradient-innovation rounded-full font-semibold text-void hover:opacity-90 transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-cyan-500/30"
      >
        <span class="relative z-10">Our Ventures</span>
      </a>
      
      <!-- Secondary CTA -->
      <a 
        href="#contact" 
        class="px-8 py-4 rounded-full font-semibold text-white border border-white/30 hover:border-white/60 hover:bg-white/5 transition-all duration-300 hover:scale-105"
      >
        Contact Us
      </a>
    </div>
  </div>
  
  <!-- Bottom Gradient Fade -->
  <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-void to-transparent"></div>
</section>
