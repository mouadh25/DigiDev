---
// Hero Section Component - Logo + lateral code animations behind content
---

<section
  class="relative min-h-screen flex items-center justify-center overflow-hidden"
>
  <!-- Animated Grid Background -->
  <div class="absolute inset-0 animated-grid opacity-20"></div>

  <!-- Large Logo Background - Discrete center -->
  <div
    class="absolute inset-0 flex items-center justify-center pointer-events-none z-0"
  >
    <div
      class="relative w-[400px] h-[400px] md:w-[500px] md:h-[500px] opacity-[0.15]"
    >
      <img
        src="/logo_symbol.png"
        alt=""
        class="w-full h-full object-contain animate-pulse-slow"
      />
    </div>
  </div>

  <!-- Radial Glow Effects -->
  <div
    class="absolute top-1/4 left-1/3 w-80 h-80 bg-cyan-500/15 rounded-full blur-3xl animate-float"
  >
  </div>
  <div
    class="absolute bottom-1/4 right-1/3 w-80 h-80 bg-lime-500/15 rounded-full blur-3xl animate-float-delayed"
  >
  </div>

  <!-- Code Animation - LEFT (behind content, closer to center) -->
  <div
    class="absolute left-[10%] md:left-[15%] lg:left-[20%] top-1/2 -translate-y-1/2 z-0 hidden md:block"
  >
    <div
      class="glassmorphism rounded-xl p-4 max-w-[240px] opacity-30 hover:opacity-60 transition-opacity"
    >
      <div class="flex items-center gap-2 mb-2">
        <div class="w-2 h-2 rounded-full bg-red-500/80"></div>
        <div class="w-2 h-2 rounded-full bg-yellow-500/80"></div>
        <div class="w-2 h-2 rounded-full bg-green-500/80"></div>
        <span class="text-xs text-white/40 ml-1 font-mono">future.ts</span>
      </div>
      <pre
        class="text-xs font-mono leading-relaxed"><code class="text-white/60"><span class="text-violet-400">const</span> <span class="text-cyan-400">future</span> = <span class="text-orange-400">await</span>
  <span class="text-lime-400">DigiDev</span>.<span class="text-cyan-300">design</span>()
    .<span class="text-cyan-300">develop</span>()
    .<span class="text-cyan-300">deploy</span>();

<span class="text-white/30">// Algeria's digital</span>
<span class="text-white/30">// tomorrow<span class="typing-cursor">_</span></span></code></pre>
    </div>
  </div>

  <!-- Code Animation - RIGHT (behind content, closer to center) -->
  <div
    class="absolute right-[10%] md:right-[15%] lg:right-[20%] top-1/2 -translate-y-1/2 z-0 hidden md:block"
  >
    <div
      class="glassmorphism rounded-xl p-4 max-w-[240px] opacity-30 hover:opacity-60 transition-opacity"
    >
      <div class="flex items-center gap-2 mb-2">
        <div class="w-2 h-2 rounded-full bg-red-500/80"></div>
        <div class="w-2 h-2 rounded-full bg-yellow-500/80"></div>
        <div class="w-2 h-2 rounded-full bg-green-500/80"></div>
        <span class="text-xs text-white/40 ml-1 font-mono">ai.py</span>
      </div>
      <pre
        class="text-xs font-mono leading-relaxed"><code class="text-white/60"><span class="text-violet-400">from</span> <span class="text-lime-400">future</span> <span class="text-violet-400">import</span> *

<span class="text-cyan-400">model</span> = <span class="text-lime-400">AI</span>.<span class="text-cyan-300">load</span>(
  <span class="text-orange-300">"innovation"</span>
)

<span class="text-cyan-400">result</span> = model.<span class="text-cyan-300">run</span>()<span class="typing-cursor">_</span></code></pre>
    </div>
  </div>

  <!-- Hero Content (z-10 = in front of code) -->
  <div class="relative z-10 text-center px-6 max-w-4xl mx-auto">
    <!-- Glassmorphism Badge -->
    <div
      class="inline-flex items-center gap-2 px-5 py-2.5 mb-8 rounded-full glassmorphism hover-glow cursor-default animate-fade-in-up"
    >
      <span class="w-2 h-2 bg-lime-500 rounded-full animate-pulse"></span>
      <span class="text-sm font-medium text-white/90 tracking-wide"
        >AI-Native Studio</span
      >
    </div>

    <!-- Main Headline -->
    <h1
      class="text-5xl md:text-7xl lg:text-8xl font-bold font-display mb-6 leading-tight animate-fade-in-up animation-delay-100"
    >
      <span class="text-gradient">We Engineer</span>
      <br />
      <span class="text-white">the Future.</span>
    </h1>

    <!-- Subheadline -->
    <p
      class="text-lg md:text-xl text-white/60 max-w-2xl mx-auto mb-12 font-body leading-relaxed animate-fade-in-up animation-delay-200"
    >
      Building Algeria's next generation of digital products.
      <br class="hidden md:block" />
      Design. Development. Automation.
    </p>

    <!-- CTA Buttons -->
    <div
      class="flex flex-col sm:flex-row items-center justify-center gap-4 animate-fade-in-up animation-delay-300"
    >
      <a
        href="#ventures"
        class="group relative px-8 py-4 bg-gradient-innovation rounded-full font-semibold text-void hover:opacity-90 transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-cyan-500/30"
      >
        Our Ventures
      </a>

      <a
        href="#contact"
        class="px-8 py-4 rounded-full font-semibold text-white border border-white/30 hover:border-white/60 hover:bg-white/5 transition-all duration-300 hover:scale-105"
      >
        Contact Us
      </a>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce z-10">
    <div
      class="w-6 h-10 rounded-full border-2 border-white/20 flex items-start justify-center p-2"
    >
      <div class="w-1 h-2 bg-white/40 rounded-full animate-scroll-indicator">
      </div>
    </div>
  </div>

  <!-- Bottom Gradient Fade -->
  <div
    class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-void to-transparent z-20"
  >
  </div>
</section>

<style>
  @keyframes pulse-slow {
    0%,
    100% {
      opacity: 0.08;
      transform: scale(1) rotate(0deg);
    }
    50% {
      opacity: 0.12;
      transform: scale(1.05) rotate(2deg);
    }
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(-20px) translateX(10px);
    }
  }

  @keyframes float-delayed {
    0%,
    100% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(20px) translateX(-10px);
    }
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes scroll-indicator {
    0%,
    100% {
      transform: translateY(0);
      opacity: 1;
    }
    50% {
      transform: translateY(8px);
      opacity: 0.3;
    }
  }

  @keyframes blink {
    0%,
    50% {
      opacity: 1;
    }
    51%,
    100% {
      opacity: 0;
    }
  }

  .animate-pulse-slow {
    animation: pulse-slow 6s ease-in-out infinite;
  }
  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
  .animate-float-delayed {
    animation: float-delayed 7s ease-in-out infinite;
  }
  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
  }
  .animate-scroll-indicator {
    animation: scroll-indicator 2s ease-in-out infinite;
  }

  .animation-delay-100 {
    animation-delay: 0.1s;
  }
  .animation-delay-200 {
    animation-delay: 0.2s;
  }
  .animation-delay-300 {
    animation-delay: 0.3s;
  }

  .typing-cursor {
    animation: blink 1s step-end infinite;
    color: #84cc16;
  }
</style>
