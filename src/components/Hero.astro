---
// Hero Section Component - Enhanced with code animation and logo background
// Per PROMPTS.md Prompt 1 + user feedback for more visual impact
---

<section
  class="relative min-h-screen flex items-center justify-center overflow-hidden"
>
  <!-- Animated Grid Background -->
  <div class="absolute inset-0 animated-grid opacity-20"></div>

  <!-- Large Logo Background - Stylized with blur and gradient mask -->
  <div
    class="absolute inset-0 flex items-center justify-center pointer-events-none"
  >
    <div
      class="relative w-[600px] h-[600px] md:w-[800px] md:h-[800px] opacity-[0.04]"
    >
      <img
        src="/logo_symbol.png"
        alt=""
        class="w-full h-full object-contain animate-pulse-slow"
        style="filter: blur(2px);"
      />
      <!-- Gradient overlay for fade effect -->
      <div
        class="absolute inset-0 bg-gradient-radial from-transparent via-transparent to-void"
      >
      </div>
    </div>
  </div>

  <!-- Radial Glow Effects -->
  <div
    class="absolute top-1/4 left-1/4 w-96 h-96 bg-cyan-500/10 rounded-full blur-3xl animate-float"
  >
  </div>
  <div
    class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-lime-500/10 rounded-full blur-3xl animate-float-delayed"
  >
  </div>
  <div
    class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-orange-500/5 rounded-full blur-3xl"
  >
  </div>

  <!-- Code Typing Animation - Left Side -->
  <div
    class="absolute left-4 md:left-12 top-1/2 -translate-y-1/2 hidden lg:block"
  >
    <div
      class="glassmorphism rounded-xl p-4 max-w-[280px] opacity-40 hover:opacity-70 transition-opacity"
    >
      <div class="flex items-center gap-2 mb-3">
        <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
        <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
        <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
        <span class="text-xs text-white/40 ml-2 font-mono">future.ts</span>
      </div>
      <pre
        class="text-xs font-mono leading-relaxed"><code class="text-white/60"><span class="text-violet-400">const</span> <span class="text-cyan-400">future</span> = <span class="text-orange-400">await</span> <span class="text-lime-400">DigiDev</span>
  .<span class="text-cyan-300">design</span>()
  .<span class="text-cyan-300">develop</span>()
  .<span class="text-cyan-300">deploy</span>();

<span class="text-white/40">// Building Algeria's</span>
<span class="text-white/40">// digital tomorrow</span>
<span class="typing-cursor">_</span></code></pre>
    </div>
  </div>

  <!-- Code Typing Animation - Right Side -->
  <div
    class="absolute right-4 md:right-12 top-1/2 -translate-y-1/2 hidden lg:block"
  >
    <div
      class="glassmorphism rounded-xl p-4 max-w-[280px] opacity-40 hover:opacity-70 transition-opacity"
    >
      <div class="flex items-center gap-2 mb-3">
        <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
        <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
        <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
        <span class="text-xs text-white/40 ml-2 font-mono">ai.py</span>
      </div>
      <pre
        class="text-xs font-mono leading-relaxed"><code class="text-white/60"><span class="text-violet-400">from</span> <span class="text-lime-400">future</span> <span class="text-violet-400">import</span> *

<span class="text-cyan-400">model</span> = <span class="text-lime-400">AI</span>.<span class="text-cyan-300">load</span>(
  <span class="text-orange-300">"innovation"</span>
)

<span class="text-cyan-400">result</span> = model.<span class="text-cyan-300">generate</span>(
  <span class="text-orange-300">"excellence"</span>
)<span class="typing-cursor">_</span></code></pre>
    </div>
  </div>

  <!-- Hero Content -->
  <div class="relative z-10 text-center px-6 max-w-5xl mx-auto">
    <!-- Glassmorphism Badge -->
    <div
      class="inline-flex items-center gap-2 px-5 py-2.5 mb-8 rounded-full glassmorphism hover-glow cursor-default animate-fade-in-up"
    >
      <span class="w-2 h-2 bg-lime-500 rounded-full animate-pulse"></span>
      <span class="text-sm font-medium text-white/90 tracking-wide"
        >AI-Native Studio</span
      >
    </div>

    <!-- Main Headline -->
    <h1
      class="text-5xl md:text-7xl lg:text-8xl font-bold font-display mb-6 leading-tight animate-fade-in-up animation-delay-100"
    >
      <span class="text-gradient">We Engineer</span>
      <br />
      <span class="text-white">the Future.</span>
    </h1>

    <!-- Subheadline -->
    <p
      class="text-lg md:text-xl text-white/60 max-w-2xl mx-auto mb-12 font-body leading-relaxed animate-fade-in-up animation-delay-200"
    >
      Building Algeria's next generation of digital products.
      <br class="hidden md:block" />
      Design. Development. Automation.
    </p>

    <!-- CTA Buttons -->
    <div
      class="flex flex-col sm:flex-row items-center justify-center gap-4 animate-fade-in-up animation-delay-300"
    >
      <!-- Primary CTA -->
      <a
        href="#ventures"
        class="group relative px-8 py-4 bg-gradient-innovation rounded-full font-semibold text-void hover:opacity-90 transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-cyan-500/30"
      >
        <span class="relative z-10">Our Ventures</span>
      </a>

      <!-- Secondary CTA -->
      <a
        href="#contact"
        class="px-8 py-4 rounded-full font-semibold text-white border border-white/30 hover:border-white/60 hover:bg-white/5 transition-all duration-300 hover:scale-105"
      >
        Contact Us
      </a>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
    <div
      class="w-6 h-10 rounded-full border-2 border-white/20 flex items-start justify-center p-2"
    >
      <div class="w-1 h-2 bg-white/40 rounded-full animate-scroll-indicator">
      </div>
    </div>
  </div>

  <!-- Bottom Gradient Fade -->
  <div
    class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-void to-transparent"
  >
  </div>
</section>

<style>
  @keyframes pulse-slow {
    0%,
    100% {
      opacity: 0.04;
      transform: scale(1);
    }
    50% {
      opacity: 0.06;
      transform: scale(1.02);
    }
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(-20px) translateX(10px);
    }
  }

  @keyframes float-delayed {
    0%,
    100% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(20px) translateX(-10px);
    }
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes scroll-indicator {
    0%,
    100% {
      transform: translateY(0);
      opacity: 1;
    }
    50% {
      transform: translateY(8px);
      opacity: 0.3;
    }
  }

  @keyframes blink {
    0%,
    50% {
      opacity: 1;
    }
    51%,
    100% {
      opacity: 0;
    }
  }

  .animate-pulse-slow {
    animation: pulse-slow 8s ease-in-out infinite;
  }
  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
  .animate-float-delayed {
    animation: float-delayed 7s ease-in-out infinite;
  }
  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
  }
  .animate-scroll-indicator {
    animation: scroll-indicator 2s ease-in-out infinite;
  }

  .animation-delay-100 {
    animation-delay: 0.1s;
  }
  .animation-delay-200 {
    animation-delay: 0.2s;
  }
  .animation-delay-300 {
    animation-delay: 0.3s;
  }

  .typing-cursor {
    animation: blink 1s step-end infinite;
    color: #84cc16;
  }

  .bg-gradient-radial {
    background: radial-gradient(circle, transparent 0%, #050505 70%);
  }
</style>
